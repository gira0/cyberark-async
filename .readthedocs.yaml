version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.12"

sphinx:
  fail_on_warning: true

python:
  # Install our python package before building the docs
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs